apiVersion: v1
kind: resourceQuota
metadata:
  name: bloodbank-quota
  namespace: bloodbankApp

spec:
  hard:
    requests.cpu: "2"
    limits.cpu: "4"
    requests.memory: 2Gi
    limits.memory: 4Gi
    pods: "10"
    services: "10"
